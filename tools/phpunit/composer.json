{
    "require": {
        "symfony/phpunit-bridge": "^6.1"
    },
    "scripts": {
        "post-install-cmd": [
            "@php vendor/bin/simple-phpunit install --configuration ../../phpunit.xml.dist"
        ],
        "post-update-cmd": [
            "@php vendor/bin/simple-phpunit install --configuration ../../phpunit.xml.dist"
        ]
    }
}
